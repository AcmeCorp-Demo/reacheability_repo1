# CVE-2024-3651 — idna < 3.7 (verified: affects 3.6)
idna==3.6

# CVE-2024-34064 — jinja2 < 3.1.4 (verified: 3.1.3 is vulnerable)
# CVE-2024-22195 — jinja2 < 3.1.3 (verified: 3.1.2 is vulnerable)
# CVE-2020-28493 — jinja2 < 2.11.3 (verified: 2.11.2 is vulnerable)
# CVE-2019-10906 — jinja2 < 2.10.1 (verified: 2.10.0 is vulnerable)
# NOTE: A single pinned version can only cover some of these.
# Use 3.1.3 to cover CVE-2024-34064 + CVE-2020-28493 + CVE-2019-10906.
# Use 2.10.0 separately if you need CVE-2019-10906 isolated.
jinja2==3.1.3

# CVE-2022-21797 — joblib < 1.1.1 (verified via Snyk: fixed in 1.1.1)
joblib==1.1.0

# CVE-2021-32862 — nbconvert < 6.5.1 (verified: fixed in 6.5.1)
nbconvert==6.5.0

# CVE-2021-34141 — numpy (disputed/minor; affects 1.19.x)
# CVE-2021-33430 — numpy (affects 1.19.x)
numpy==1.19.4

# CVE-2017-11424 — pyjwt < 1.5.2 (verified: affects 1.5.0)
PyJWT==1.5.0

# CVE-2024-36039 — pymysql <= 1.1.0 (verified: fixed in 1.1.1)
pymysql==1.1.0

# CVE-2024-5206 — scikit-learn < 1.5.0 (verified: fixed in 1.5.0)
scikit-learn==1.4.2

# CVE-2023-25399 — scipy < 1.11 (memory leak; affects 1.10.x)
# CVE-2023-29824 — scipy (use-after-free; affects 1.10.x)
scipy==1.10.0

# CVE-2023-28370 — tornado < 6.3.2 (verified: 6.2 is vulnerable)
tornado==6.2

# CVE-2021-28363 — urllib3 < 1.26.4
# CVE-2021-33503 — urllib3 < 1.26.5
# CVE-2023-43804 — urllib3 < 1.26.17 or < 2.0.5
# CVE-2023-45803 — urllib3 < 1.26.18
# CVE-2024-37891 — urllib3 < 1.26.19 or < 2.2.2
# Use 1.26.3 to trigger ALL five urllib3 CVEs with a single pin
urllib3==1.26.3

# CVE-2019-14806 — werkzeug < 0.15.3 (verified: 0.14.1 / 0.15.2 affected)
# CVE-2023-25577 — werkzeug < 2.2.3
# CVE-2023-23934 — werkzeug < 2.2.3
# Can't satisfy all three with one version. Use 2.2.2 for the 2023 CVEs.
# Add a comment for 2019-14806 requiring a separate older pin if needed.
werkzeug==2.2.2
# For CVE-2019-14806 specifically, use: werkzeug==0.14.1
#CVE-2020-28975 
pycparser==2.20
